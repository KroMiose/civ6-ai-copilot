# Snapshot 示例

示例 fixture：

- `tests/fixtures/minimal-player-visible.snapshot.json`

该 fixture 用于 schema、多人工平检查、bridge、summary、preflight 和 release gate 的自动化测试。真实游戏导出的 `*.snapshot.json` 属于本地运行时产物，由 `.gitignore` 拦截。
